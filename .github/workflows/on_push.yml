name: content in push
on: push
jobs:
  check-commit-message:
    runs-on: ubuntu-latest
    steps:
      - name: Check commit message
        env:
          COMMITS: "${{ toJson(github.event.commits) }}"
        run: |
          python scripts/test_commit_msg.py